# EZ-B001 — Historical Nuclear Mass Prediction

Legacy alias: `ZME-B001`.

Future truth must not be available to the prediction process.

```text
prepare-targets  -> identity-only targets from a later AME edition
freeze           -> KnowledgeFreeze over an older AME edition
predict          -> fit + certificates (no later-truth argument)
finalize         -> LEDGER_FINALIZED
score            -> unlock later truth, only after finalization
```

Default model: `EZ-SEMF-GP-RESIDUAL-v1`.
Optional control: `EZ-GP-DIRECT-v1`.
Physics-only: `EZ-SEMF-LS-v1`.
